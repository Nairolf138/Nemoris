{
  "name": "@capsule/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "tsc -p tsconfig.json && node --experimental-specifier-resolution=node --test tests/useCases.test.mjs"
  }
}
