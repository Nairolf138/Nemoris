{
  "name": "@capsule/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "start": "tsc -b && node dist/server.js",
    "start:dev": "tsc -b && node --watch dist/server.js",
    "test": "tsc -b && node dist/tests/run-tests.js"
  },
  "dependencies": {
    "@capsule/observability": "0.1.0",
    "@capsule/core": "0.1.0",
    "@capsule/export": "0.1.0"
  }
}
