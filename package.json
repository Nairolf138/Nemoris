{
  "name": "nemoris",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc -b",
    "test": "npm run -w @capsule/api test",
    "check:no-dist-imports": "node scripts/check-no-dist-imports.mjs",
    "lint:openapi": "ruby scripts/lint-openapi.rb",
    "docs:contract:check": "node scripts/check-contract-docs.mjs",
    "build:artifacts": "node scripts/build-artifacts.mjs",
    "version:plan": "node scripts/plan-version.mjs",
    "release:readiness:check": "node scripts/check-release-readiness.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
